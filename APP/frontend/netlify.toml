[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  # Переменные окружения для продакшена
  VITE_API_URL = "https://your-future-backend.com"  # Заменишь позже